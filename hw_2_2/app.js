/* Дано 2 числа, a и b. Их нужно прочитать с помощью prompt(). Если a < b - вывести YES иначе выведите NO. (alert-ом) */

let a = prompt()
let b = prompt ()
if (a < b) {
  alert("YES")
}
  else {
    alert ("NO")
}
